<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 启动窗口背景与 MainActivity 容器同色，消除冷启动白屏闪烁。 -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
